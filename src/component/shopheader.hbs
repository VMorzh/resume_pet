<section>
    <div class="col-12">
        <div class="card">
            <div class="nav nav-tabs d-flex justify-content-between">
                <h4 class="nav-item pt-1 ps-4 pe-5">Logo</h4>
                <div class="d-flex justify-content-end">
                    {{#each navigation.links}}
                    <div class="nav-item">
                        <a href="{{href}}" class="nav-link">{{text}}</a>
                    </div>
                    {{/each}}
                    <span class="nav-link disabled">Shop</span>
                </div>
            </div>
        </div>
    </div>
</section>